extends ../layout

include ../mixins
include event-fields

block content
  .page-header
    h3 Редактировать событие

  form.form-horizontal(role='form', method='POST', ng-controller='EditEventCtrl', ng-init='init(#{JSON.stringify(event)})', ng-submit='submit()', name='eventForm', novalidate)
    .alert.alert-warning {{event}}
    +eventFields(false)